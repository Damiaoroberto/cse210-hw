// Exceeding Requirements: Added gamified scoring system, checklist progress tracking, and modular design with inheritance & polymorphism.

class Program
{
    static void Main(string[] args)
    {
        GoalManager manager = new GoalManager();
        manager.Start();
    }
}
